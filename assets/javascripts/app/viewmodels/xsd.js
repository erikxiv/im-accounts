define([], function () {
    return {
        displayName: 'XML Schema',
        attached: function (view, parent) {
        },
        compositionComplete: function(view, parent) {
        }
    };
});